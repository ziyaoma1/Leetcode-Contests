class Solution:
    def firstStableIndex(self, nums: list[int], k: int) -> int:
        m1 = nums[0]
        for i in range(len(nums)):
            if nums[i] > m1:
                m1 = nums[i]
            m2 = min(nums[i:])
            if m1 - m2 <= k:
                return i
        return -1
